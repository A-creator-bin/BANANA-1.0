# BANANA-1.0
A programming language based on Rust, which is compiled to C and then to machine code. It is written in Python 3, using a hierarchy system: root>a>b>c to control variables and functions. Examples are available in test/.
